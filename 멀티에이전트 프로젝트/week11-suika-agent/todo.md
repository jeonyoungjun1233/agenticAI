# Week 11 TODO

## 준비 파일 확인

- [x] `AGENTS.md` 확인
- [x] `GEMINI.md` 확인
- [x] `context.md` 확인
- [x] `todo.md` 확인

## Week 11 체크리스트

- [x] 일정공지 에이전트 예시를 수박게임 작업공지 에이전트 주제로 바꾸기
- [x] `context.md`와 `todo.md`를 코딩 에이전트 맥락으로 제공하기
- [x] 바이브코딩 프롬프트를 `vibe_prompt.md`에 기록하기
- [x] `my_agent.py` 첫 버전 만들기
- [x] 코딩 에이전트 작업 방식을 "계획 먼저, 코드 나중"으로 정리하기
- [x] 외부 패키지 없이 실행되게 만들기
- [x] 최소 3개 함수가 역할별로 나뉘게 만들기
- [x] 실행 중간 결과를 출력하게 만들기
- [x] 규칙 기반 v0와 이후 LLM/외부 도구 확장의 차이를 정리하기
- [x] 12주차 확장 방향 결정하기

## 12주차 확장 결정

- [x] 12주차에는 Groq API를 먼저 붙인다.
- [x] 외부 도구 연결은 Groq API로 요약/계획 생성이 안정된 뒤에 붙인다.

선택 이유:

Groq API는 현재 규칙 기반 보고서를 자연어 개선안으로 확장하기 좋다. 외부 도구는 브라우저 검증, 파일 변경 추적, GitHub 이슈 작성처럼 범위가 커지므로 13주차 이후 확장 후보로 둔다.

## 실행 결과를 보고 남긴 다음 개선 항목

- [x] `python .\my_agent.py` 실행 확인
- [x] 콘솔 중간 결과 6단계 출력 확인
- [x] `agent_output.md` 생성 확인
- [x] `python -m py_compile .\my_agent.py` 문법 검사 통과
- [ ] Groq API 키를 코드에 직접 쓰지 않고 환경변수 `GROQ_API_KEY`로만 읽기
- [ ] 현재 규칙 기반 계획을 LLM 프롬프트 입력으로 넘겨 더 자연스러운 개선 보고서 만들기
- [x] `agent_output.md`에 "게임 코드 수정 없음" 검증 항목을 더 명확히 넣기
- [ ] 외부 도구 확장 후보를 브라우저 QA, GitHub 이슈 작성, 파일 변경 요약으로 나누어 비교하기
- [ ] 사용자가 원하는 말투로 결과 보고서를 바꾸는 옵션 추가하기

## 주의할 점

- Week 11에서는 `src/main.js`, `src/style.css`, `index.html`, `public`, `package.json`, `package-lock.json`을 수정하지 않는다.
- 이 폴더는 다른 에이전트 폴더로 옮길 수 있어야 하므로, `my_agent.py`는 자기 폴더의 문서를 우선 읽는다.
